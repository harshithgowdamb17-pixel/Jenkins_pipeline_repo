pipeline {
    agent any

    triggers {
        cron('H 2 * * *')
    }

    stages {

        stage('Prepare Backup Directory') {
            steps {
                sh '''
                mkdir -p backup
                '''
            }
        }

        stage('Create Timestamp') {
            steps {
                sh '''
                date > backup/Timestamp
                '''
            }
        }

        stage('Backup Jenkins Home') {
            steps {
                sh '''
                DATE=$(date +%F)
                tar -czf backup/jenkins_backup_$DATE.tar.gz /var/lib/jenkins
                '''
            }
        }

    }
}